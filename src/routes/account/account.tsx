import "./account.css";

const Account = () => {
  return (
    <section id="account">
      <button>Payment Portal</button>
      <button>Contact Us</button>
    </section>
  );
};

export default Account;
